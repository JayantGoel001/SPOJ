#include<bits/stdc++.h>
#define int long long int
using namespace std;
int32_t main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int t=1;
    cin>>t;
    while(t--){
        int l,c,s;
        cin>>l>>c>>s;
        for (int i = 0; i <l*(2*s); ++i) {
            for (int j = 0; j <= c*(s+1); ++j) {

            }
            cout<<"\n";
        }
        cout<<"\n";
    }
}